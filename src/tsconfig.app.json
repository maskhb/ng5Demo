{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "../out-tsc/app",/* 重定向输出目录。 */
    "baseUrl": "./",/* 解析非相对模块名的基准目录  http://www.tslang.cn/docs/handbook/module-resolution.html*/
    "module": "es2015",/* 	指定生成哪个模块系统代码：'commonjs'，'amd'，'system'，或 'umd'或'es2015'。只有'amd'和'system'能和--outFile一起使用。当目标是ES5或以下的时候不能使用'es2015'。 */
    "types": []/* 要包含的类型声明文件名列表。 */
  },
  "exclude": [/* 不匹配 */
    "test.ts",
    "**/*.spec.ts",
    "testing/**"
  ]
}
